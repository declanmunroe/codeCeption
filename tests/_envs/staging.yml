# `staging` environment config goes here

actor: AcceptanceTester
modules:
    enabled:
        - WebDriver:
            url: 'http://ics.staging/'
            window-size: maximise
            port: 9515
            browser: chrome
            