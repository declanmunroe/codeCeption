# `dev` environment config goes here

actor: AcceptanceTester
modules:
    enabled:
        - WebDriver:
            url: 'https://www.ics.ie/ecf/index/index/id/1/jobid/0'
            window-size: maximise
            port: 9515
            browser: chrome
            paths:
                envs: tests/_envs
            
            