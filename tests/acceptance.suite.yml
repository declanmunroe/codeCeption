# Codeception Test Suite Configuration
#
# Suite for acceptance tests.
# Perform tests in browser using the WebDriver or PhpBrowser.
# If you need both WebDriver and PHPBrowser tests - create a separate suite.

actor: AcceptanceTester
modules:
    enabled:
        - WebDriver:
            url: 'http://ics.staging/'
            window-size: maximise
            port: 9515
            browser: chrome
            paths:
                envs: tests/_envs
    

env:
    baai:
        modules:
            config:
                WebDriver:
                    url: 'http://businessanalyst.staging/' 
                    browse: chrome

                

    dpo:
        modules:
            config:
                WebDriver:
                    url: 'http://dpo.staging/' 
                    browse: chrome




    iasa:
        modules:
            config:
                WebDriver:
                    url: 'http://iasa.staging/' 
                    browse: chrome 




    hisi:
        modules:
            config:
                WebDriver:
                    url: 'http://hisi.staging/' 
                    browse: chrome





    itsmf:
        modules:
            config:
                WebDriver:
                    url: 'http://itsmf.staging/' 
                    browse: chrome


                
    
            

            
